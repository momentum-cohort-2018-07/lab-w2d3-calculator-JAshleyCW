var firstNumber = 3;
var secondNumber = 7;
var total = Number(firstNumber) + Number(secondNumber);
alert (tatal);


// set var for each button
// click button and store value in an array -- var myArray []; empty array to push numbers to -- call index #
// create a loop to 'loop' through each number to determine which ones to store in the array ??
// array method forEach()